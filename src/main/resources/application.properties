spring.application.name=config-service
spring.cloud.config.server.git.uri=https://github.com/mangeeru/config-server
management.endpoints.web.exposure.include=*
server.port=8089

#Below configuration will load application.property file based on profile
#we have to configure below configuration in client application, it optional
#spring.profiles.active=dev
#we have to configure below configuration in client application
#spring.cloud.config.profile=dev
